int main(int a) {
    char sourceString[50];
    char destinationString[50];
    char a;
    float f;
    int index;

    while (sourceString[index] != a) {
        destinationString[index] = sourceString[index];
        index++;
    }
    destinationString[index] = '\0';

    print("Source String: %s\n", sourceString);
    print("Copied String: %s\n", destinationString);

    return 0;
}
